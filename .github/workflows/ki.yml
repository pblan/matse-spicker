name: Einführung in Künstliche Intelligenz

on:
  push:
    paths:
      - bachelor/ki/**
  pull_request:
    paths:
      - bachelor/ki/**

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  compile:
    uses: pblan/matse-spicker/.github/workflows/compile.yml@main
    with:
       spicker-handle: ki
       spicker-fullname: Einführung in Künstliche Intelligenz