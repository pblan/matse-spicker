name: Softwaretechnik

on:
  push:
    paths:
      - bachelor/swt/**
  pull_request:
    paths:
      - bachelor/swt/**

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  compile:
    uses: pblan/matse-spicker/.github/workflows/compile.yml@main
    with:
       spicker-handle: swt
       spicker-fullname: Softwaretechnik