name: Statistische Modellierung

on:
  push:
    branches:
      - main
      - development
    paths:
      - master/sm/**
  pull_request:
    branches:
      - main
      - development
    paths:
      - master/sm/**

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  compile:
    uses: pblan/matse-spicker/.github/workflows/compile.yml@main
    with:
       spicker-handle: sm
       spicker-fullname: Statistische Modellierung