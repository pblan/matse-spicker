name: Kommunikationssysteme

on:
  push:
    paths:
      - bachelor/kosy/**
  pull_request:
    paths:
      - bachelor/kosy/**

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  compile:
    uses: pblan/matse-spicker/.github/workflows/compile.yml@main
    with:
       spicker-handle: kosy
       spicker-fullname: Einführung in Kommunikationssysteme