name: Web-Engineering und Internettechnologien

on:
  push:
    branches:
      - main
      - development
    paths:
      - bachelor/weit/**
  pull_request:
    branches:
      - main
      - development
    paths:
      - bachelor/weit/**

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  compile:
    uses: pblan/matse-spicker/.github/workflows/compile.yml@main
    with:
       spicker-handle: weit
       spicker-fullname: Web-Engineering und Internettechnologien