\begin{tikzpicture}
\begin{axis}[%
        xlabel=$x$,
        ylabel=$F(x)$,
        grid=both,
        axis x line = bottom,
        axis y line = left,
        jump mark left,
        discontinuous,
    ]
    \addplot table [y=cumulative distribution]{includes/figures/example_gleichverteilung.csv};
\end{axis}
\end{tikzpicture}